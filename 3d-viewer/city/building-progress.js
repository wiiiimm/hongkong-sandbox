export function coveragePresentation(data){
 if(data?.version!==1||data.status!=='available'||data.unit!=='source-building-form'||!Number.isInteger(data.totalForms)||!Number.isInteger(data.reviewedEnhancedForms)||data.totalForms<0||data.reviewedEnhancedForms<0||data.reviewedEnhancedForms>data.totalForms||!Number.isFinite(Date.parse(data.updatedAt)))return null;
 const percent=data.totalForms?data.reviewedEnhancedForms/data.totalForms*100:null;
 return{total:data.totalForms,enhanced:data.reviewedEnhancedForms,percent,date:new Date(data.updatedAt).toLocaleDateString('en-HK',{day:'numeric',month:'short',year:'numeric',timeZone:'Asia/Hong_Kong'}),percentage:percent===null?'Not available':`${percent.toLocaleString('en-HK',{maximumFractionDigits:2,minimumFractionDigits:2})}%`};
}
export async function bindBuildingProgress(manifest,{doc=document,fetcher=fetch}={}){
 const dialog=doc.getElementById('building-progress'),open=doc.getElementById('progress-open'),close=doc.getElementById('progress-close');open.addEventListener('click',()=>dialog.showModal());close.addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close();}});
 const unavailable=message=>{doc.getElementById('progress-status').textContent=message;doc.getElementById('progress-values').hidden=true;doc.getElementById('progress-chip').textContent='Updating';};
 try{const response=await fetcher('city/data/building-progress.json',{cache:'no-cache'});if(!response.ok)throw Error('Statistics unavailable');const data=await response.json(),p=coveragePresentation(data);if(!p)throw Error('Statistics unavailable');const digest=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(JSON.stringify(manifest))),actual=[...new Uint8Array(digest)].map(x=>x.toString(16).padStart(2,'0')).join('');if(actual!==data.manifestDigest){unavailable('Statistics are being updated for this map version.');return;}
  const number=n=>n.toLocaleString('en-HK');doc.getElementById('progress-total').textContent=number(p.total);doc.getElementById('progress-enhanced').textContent=number(p.enhanced);doc.getElementById('progress-percent').textContent=p.percentage;doc.getElementById('progress-ratio').textContent=`${number(p.enhanced)} of ${number(p.total)} source building forms`;const meter=doc.getElementById('progress-meter');meter.max=p.total||1;meter.value=p.enhanced;meter.setAttribute('aria-valuetext',`${p.percentage}: ${number(p.enhanced)} of ${number(p.total)} source building forms have reviewed enhanced geometry`);meter.hidden=p.percent===null;doc.getElementById('progress-date').textContent=p.date;doc.getElementById('progress-chip').textContent=`${p.percentage} · ${number(p.enhanced)} / ${number(p.total)}`;open.setAttribute('aria-label',`Model Enhancement Progress: ${p.percentage}, ${number(p.enhanced)} enhanced of ${number(p.total)} source building forms`);doc.getElementById('progress-status').textContent='Citywide totals for this map version';doc.getElementById('progress-values').hidden=false;
 }catch{unavailable('Building statistics are temporarily unavailable. You can still explore the city.');}
}
