# Cloudflare Pages headers.
# Allow the viewer to be embedded anywhere via <iframe> (HKS-27) — this is a
# public, shareable map, so framing is intentional (no X-Frame-Options: DENY).
/*
  Content-Security-Policy: frame-ancestors *
