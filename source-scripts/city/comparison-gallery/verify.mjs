import {createRequire} from 'node:module';import {writeFile} from 'node:fs/promises';import assert from 'node:assert/strict';
const require=createRequire(new URL('../../../3d-viewer/city/package.json',import.meta.url)),{chromium}=require('playwright'),out=new URL('../../../docs/astra-city/comparison-gallery/',import.meta.url);const browser=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',args:['--enable-webgl','--ignore-gpu-blocklist']}),report={errors:[],views:[]};
try{for(const width of [1440,390]){const page=await browser.newPage({viewport:{width,height:1000},reducedMotion:'reduce'});page.on('pageerror',e=>report.errors.push(e.message));await page.goto('http://127.0.0.1:4176/whampoa-comparison.html');await page.waitForFunction(()=>window.__trial?.ready);await page.click('#gallery-toggle');await page.waitForFunction(()=>window.__gallery?.active&&window.__gallery.drawn>0);assert.equal(await page.locator('.gallery-cell').count(),21);assert.equal(await page.locator('.gallery-canvas').count(),1);assert.equal(await page.evaluate(()=>window.__gallery.playing),false);const before=await page.evaluate(()=>window.__gallery.locations.map(r=>r.camera));await page.click('#gallery-play');await page.waitForTimeout(500);assert.notDeepEqual(await page.evaluate(()=>window.__gallery.locations.map(r=>r.camera)),before);await page.click('#gallery-play');await page.waitForTimeout(100);const paused=await page.evaluate(()=>window.__gallery.locations.map(r=>r.camera));await page.waitForTimeout(400);assert.deepEqual(await page.evaluate(()=>window.__gallery.locations.map(r=>r.camera)),paused);await page.selectOption('#gallery-motion','fly');await page.locator('#gallery-speed').fill('2');await page.locator('#gallery-speed').dispatchEvent('input');await page.waitForTimeout(100);assert.equal(await page.evaluate(()=>window.__gallery.speed),2);await page.screenshot({path:new URL('gallery-'+width+'.png',out).pathname});if(width===1440){const last=await page.locator('.gallery-row').last().boundingBox();assert(last.y+last.height<=1000);}else{await page.locator('.gallery-row').last().scrollIntoViewIfNeeded();await page.waitForTimeout(150);await page.screenshot({path:new URL('gallery-mobile-bottom.png',out).pathname});assert((await page.evaluate(()=>window.__gallery.drawn))>0);}assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await page.locator('#gallery-toggle').scrollIntoViewIfNeeded();await page.click('#gallery-toggle');assert(await page.locator('#views').isVisible());await page.click('#flythrough');await page.waitForTimeout(200);assert(await page.evaluate(()=>window.__trial.flying));await page.click('#flythrough');await page.click('#gallery-toggle');await page.waitForFunction(()=>window.__gallery.active);assert.equal(await page.locator('.gallery-canvas').count(),1);report.views.push({width,cells:21,sharedRenderers:1,pauseStable:true,speed:2,returnToTour:true});await page.close();}assert.deepEqual(report.errors,[]);}finally{await browser.close();await writeFile(new URL('verification.json',out),JSON.stringify(report,null,2)+'\n');}console.log(JSON.stringify(report));
