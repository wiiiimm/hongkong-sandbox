# HKS-216 / HKS-217 · Current model-pass working cache

This is a **local inventory awaiting the final integration commit**, not an uploaded checkpoint. Run `collect_incremental.py --prior-manifest /path/to/verified-prior-manifest.json` from the source checkout. It validates the manifest SHA against `R2-CLOUD-CHECKPOINT.json`, hashes the explicit current-pass roots, and checks local glTF buffer and content-addressed catalogue dependencies. The compressed inventory retains the exact paths and hashes; regenerate after active writers finish.

Initial scan: no missing paths or unresolved model/buffer references. About 195.5 MB of additional unique objects versus 1.12 GB reused from the previous checkpoint. Counts are provisional while other agents finish their caches. Git-tracked assets are restored from the final matching commit. Exact model copies in several staging locations share one immutable R2 object. Relocated catalogue references explicitly name another checked copy; they are not missing geometry.

Keep the existing checkpoint `14dfae786d71b0a1f647284720c19507103791ad2b1c5d8348a4547ec12a2601` and its source commit unchanged. The new full manifest must link to it and retain its historical SQLite object without uploading the changed 640 MB local SQLite file. **That database remains a historical snapshot. Current selections/review states are in the chosen Neon branch and must be retrieved with the current shared-modelling inventory/review-ledger tools.** Do not run legacy SQLite mutation/staging commands assuming its old job rows describe this pass. Rebuilding a fresh legacy database from current footprint/runtime manifests is a separate explicit migration, not implicit restore behaviour.

Upload only after the parent supplies the final commit SHA. Use existing `r2_snapshot.snapshot` to produce the current local object manifest, then existing `remote_checkpoint.transfer` on only hashes absent from the verified parent manifest. It verifies every newly transferred object by remote readback. Publish a full restore manifest only after this succeeds, including unchanged object references, the historical SQLite row, parent manifest key/SHA and the current Git SHA. Running the unmodified full `remote_checkpoint upload` would unnecessarily download all old objects again.

The full manifest remains consumable by the existing restore operation. A clean restore needs the matching Git checkout and must retrieve all referenced objects, verify their hashes, reconstruct links, and rerun model/buffer closure checks. A successful source-device inventory alone is not clean-device verification. Record the current Neon snapshot/branch separately from the historical SQLite provenance.

Explicit roots exclude credentials, Git internals, node_modules, Python caches and media. Existing tracked browser evidence comes from Git. This pass includes additional identity, omitted-support and native terrain inputs plus generated staging assets; optional full-resolution review images are not included. Source URLs are not parsed or copied into this inventory; cached source metadata remains within the authorised project source closure.
