# Residual native assembly review — HKS-214

Ten exact government source components are approved for integration, with two bounded native terrain patches: Island Resort mall and six towers, its neighbouring lift shaft179500, Pacifica mall, and Wplace. No region or complete estate is claimed finished. `approval.json` is the decision; `source-scripts/city/residual-support-review/publication-plan.json` is the guarded package. Root remains the single publisher.

The script audited38 reserved components and13 supporting podiums. Twenty-three candidates passed two actual browser views each, with camera occupancy, source-ray framing, full bounds, active native loading and browser-error checks. The two additional exact-CSUID neighbours also passed framing. Exported contact sheets and source screenshots were visually inspected. Source heights and model geometry remain unchanged at1× vertical scale. Window shading is illustrative.

Terrain and neighbouring structures are separate acceptance gates. The4 candidate patches cover129 existing source footprints in finite edge/centre sampling. The LOHAS extension retains all29,751 prior Club Galaxy nodes bit-for-bit. However, native ancillary block272501 remains1.867–1.871m above even the exact source TIN, so that full extension and its13 candidate components remain held. Two unsurveyed canopy-base updates are prepared but excluded from the approved plan. Festival306084 is held because neighbouring retained towers would float over the finer terrain and its native podium does not supply their support. No invented supports, model offsets or height multipliers were applied.

Island Resort contains0.009765625m² of overlapping projected native terrain faces. The same excess is present in the checksummed original TIN; nine actual Float32 highest-ray comparisons agree within8.9e-15m. This evidence is attached by the existing overlap gate without changing any position/index bytes. Pacifica has no material projected overlap. Both approved patch boundaries retain parent heights (largest measured boundary delta0.000053m).

Validation: the existing publisher dry-run passes for10 new models,2 exact terrain patches and0 base updates. Report: `publisher/publication-plan.json`. Full and isolated browser evidence: `framing/report.json`, `extras-framing/report.json`; all25 framed source components passed, but only10 are approved in this package. `support-terrain.json`, `native-audit.json`, `neighbours.json`, `context-contacts.json` and `extras-native.json` retain the held evidence. The two new neighbours have an append-only `supplemental-preflight.json` for the parent's next immutable Neon review inventory.

Reproduction: run `prepare.py`, `review.mjs`, `export.mjs`, `native_audit.py`, `prepare_terrain.py`, `exact.py`, then `preserve.py`. Run `review.mjs --terrain-bundle docs/astra-city/residual-support-review/terrain-bundle.json --output docs/astra-city/residual-support-review/support-terrain.json`, `prepare_visual.py`, `browser.mjs`, and `contact.mjs`. The adjacent, neighbour, extras and overlap scripts provide the follow-up evidence. `preserve.py` requires fresh unblended output from `prepare_terrain.py`; it must not be applied repeatedly to an already blended patch. `overlap.py` likewise starts from an unannotated native patch. `finalise.py` builds only the accepted subset. All commands require the canonical source reservations; generated evidence is not automatic publication permission. The review-only decoded mesh cache is ignored and reproducible from retained source assets.
