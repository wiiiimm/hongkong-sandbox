# Grand Promenade native support closure — HKS-214

The approved package adds the exact native Grand Promenade podium and Towers 3 and 5, plus the neighbouring Marine Police Regional Headquarters. Three already installed tower assets remain byte-identical; only their support dependencies change from the basic surveyed podium to the exact native podium. This closes all five tower-to-podium seams without rendering conflicting basic/native support forms.

All 200 low-rim samples have terrain or exact native triangle contact. Full and isolated captures cover seven native source components, and the exported images were inspected. The podium's roof framework, courts, stepped decks and tower sockets, the five articulated tower crowns, and the Headquarters' rounded waterfront wing, sloped main roof and rooftop equipment remain unchanged source geometry. Approval concerns source components, not the whole surrounding district.

The 280 × 280 m terrain patch has complete native coverage at all 78,961 grid nodes, sampled from the checksummed government TIN. All 1,124 boundary samples retain parent heights within 0.00000043 m. The neighbouring footprint audit covers 53 source records. The Headquarters' simplified envelope warning is resolved by its actual source geometry: three lowest native rim vertices lie 0.23–0.31 m into the final terrain and the remaining building/coastal frontage were visually inspected. Native apex 20.594 m differs from the older footprint top 14.3 m; the source roof and service geometry are retained without scaling.

Two open-sided structures have null surveyed base/top values and a 3 m estimated height. Their bases change from 5 m to 3.766 m and 3.481 m, respectively, using exact footprint/terrain-triangle minima. Their height, source fields and outlines remain unchanged. Both canopies were inspected in normal and explicitly isolated runtime-geometry views, with grounded posts.

The first staging pass removed three old catalogue entries without updating its count, which rejected that surrounding catalogue. The harness was corrected and all six assembly views plus canopy checks were repeated. Final evidence has no runtime catalogue errors, no support holds and clear target framing. The separate Headquarters source review kept unmodified surrounding catalogues throughout.

`source-scripts/city/residual-support-review/grand/publication-plan.json` passes the existing publisher dry-run: four new models, three metadata migrations, one terrain patch and two guarded estimate updates. `approval.json`, `support-neighbour.json`, the three browser report folders and `publisher/publication-plan.json` retain evidence. The parent is the only publisher and manages the Neon review ledger. `supplemental-preflight.json` adds the newly discovered Headquarters source to the next immutable inventory.

The bounded Headquarters acquisition reused the existing script with batch `grand-promenade-neighbour-20260909`, a 10 MB cap and one worker. It transferred 1,473,863 bytes; the exact compact model is 21,722 bytes. Source and provenance live beside that acquisition batch. No model offsets, terrain exaggeration or invented foundations were used.
